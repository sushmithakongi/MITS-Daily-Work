version: '3.8'
services:
  #Nginx Service
   webserver:
    #  build:
          # dockerfile: Dockerfile
     image: charitha/frontend:v1
     container_name: 40f2fce72cd3
     ports:
       - 86:80
   phpmyadmin:
    image: charitha/backend:5.0.1
    restart: always
    environment:
      PMA_HOST: mysql-server
      PMA_USER: root
      PMA_PASSWORD: secret
    ports:
      - "8080:80"
