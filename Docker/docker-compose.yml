version: '3.8'
services:
  #Nginx Service
   webserver:
    #  build:
          # dockerfile: Dockerfile
     image: faizasara/frontend:frontendimg
     container_name: 40f2fce72cd3
     ports:
       - 86:80
   flask:
      #  build:
      #       context: ../.
      #       dockerfile: Dockerfile
       image: faizasara/backend:backendimg
       container_name: 598d450b11f9
       command: python -m flask run
       ports:
            - 5000:3000